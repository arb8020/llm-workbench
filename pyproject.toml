[tool.uv.workspace]
members = ["bifrost", "broker", "engine"]

[project]
name = "llm-workbench"
version = "0.1.0"
description = "LLM workbench with bifrost, broker, and inference engine components"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"